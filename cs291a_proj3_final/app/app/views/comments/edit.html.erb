<h1>Edit Comment</h1>
<%= form_with(model: [@user, @post, @comment], url: user_post_comment_path(@user, @post, @comment), method: :patch, local: true) do |form| %>
  <div>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>